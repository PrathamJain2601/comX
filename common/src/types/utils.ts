export enum Role {
    OWNER = 'OWNER',
    ADMIN = 'ADMIN',
    MEMBER = 'MEMBER',
    QUEUE = 'QUEUE',
    BANNED = 'BANNED'
}

export enum Scope {
    PUBLIC = 'PUBLIC',
    PRIVATE = 'PRIVATE',
  }